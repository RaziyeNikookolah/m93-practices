۱)در فایل sample خط ۱۰ هیچ زمانی اجرا نمیشود چون بعد از return است
۲)برای age محدودیت حداکثر وجود ندارد و کاربر میتواند با این مقادیر هم person بسازد:
p3 = Person("sara", "babayi", 1000)
۳)بهتر بود برای name , lastname هم اعتبارسنجی داشتیم تا "" نگیرد و مقادیر معتبر بگیرد
۴)اینکه از تاریخ تولد به جای age استفاده کند بهتر است چون در سال های بعد داده ها اعتباری ندارند 
۵)در فایل sample سطح اولیه logging اینفو میباشد که در خط ۱۰ اگر بعد از return هم نبود بالاخره چاپ نمیشد
۶)بهتر بود برای test کلاس person فایل دیگری با نام مشخص و مرتبط با عملیات تست استفاده میشد , 
۷)تا این مقدار از کد ارتباط معنایی بین person , sample وجود ندارد پس دلیلی ندارد درون sample از کلاس person شی بسازیم یا تست نویسی person را در sample انجام دهیم
۸)اعتبارسنجی age با این ورودی ها هم p3 = Person("gholi", "gholami", True) اشکال دارد و با اجرای این کد خطایی نمیگیرد
۹)همچنین برای این ورودیها هم اعتبار سنجی وجود ندارد p3 = Person(None, None, True)
۱۰)بهتر است لاگ ها نشان داده نشوند و به سرور ارسال شوند
۱۱) این کد میتواند age تغییر دهد p2._age=23
۱۲)بهتر بود تست sub در فایل دیگری انجام میشد
۱۳)و نام تابع sub